object AndroidDataBinding {

    private const val runtimeVersion = "8.1.0"
    private const val datastoreVersion = "1.0.0"

    const val runtime = "androidx.databinding:databinding-runtime:${runtimeVersion}"
    const val dataStorePref = "androidx.datastore:datastore-preferences-core:${datastoreVersion}"
}